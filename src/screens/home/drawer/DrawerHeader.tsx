import {DrawerHeaderProps} from '@/react-navigation/drawer/lib/typescript/@types';
import React, {ReactNode} from 'react';
import {
  SafeAreaView,
  ScrollView,
  View,
  ViewStyle,
  Pressable,
  GestureResponderEvent,
  Text,
} from 'react-native';

export function DrawerHeader(props: DrawerHeaderProps): JSX.Element {
  return <Text>Edit Your Feed</Text>;
}
